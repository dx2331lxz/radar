{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch radar",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/radar",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "CMake: build"
        }
    ]
}